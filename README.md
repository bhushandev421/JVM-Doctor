A developer-facing CLI tool that attaches to a running JVM process (by PID) and produces a doctor-style diagnosis:

“Top risks” ranked by a transparent scoring model

actionable recommendations

exportable reports (console + JSON + Markdown)
